procedure Example {
  x = 2 + 10;
  z = 3;
  i = 5; 
  while (!(x==0) && (x >= 2) || (y<3)) {
    x = x - 1;
    if (x==1) then {
      z = x + 1; }
    else {
      y = z + x; }
    z = z + (x + i);
    x = (5 * 4 + 3 *2) -1;
    call q;
    i = i - 1; }
  call p; }

procedure p {
  if (x<0) then {
    while (i>0) {
      x = z * 3 + 2 - y;
      call q;
      i = i - 1; }
    x = x + 1;
    z = x + z; }
  else {
    z = 1; }
  z = z + x + i; }

procedure q {
  if (x==1) then {
    z = x + 1; }
  else {
    x = z + x; } }
