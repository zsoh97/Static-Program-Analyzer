procedure main {
    while (a == 0) {
        while (b >= 1) {
            while (c <= 2) {
                while (d > 3) {
                    while (e < 4) {
                        if (!(f + g * h + i > j - k % l / m)) then {
                            if (((n > o) || (p < q)) && (!(r == s))) then {
                                t = 12321 + 00100 + u;
                                v = a % b * (c + d) + e * f / (g - h);
                                w = 1 + 1 * 1 + 1 * 1 + 1 * 1 + 1;
                                x = (a * ( b / (c + d)) + e) * f;
                            } else {
                                while ((((aa == bb) && (cc == dd)) || (!(ee == ff))) && (hh == ii)) {
                                    print b;
                                }
                            }
                        } else {
                            print a;
							call one;
                        }
                    } 
					call two;
                }
            } 
        }
    }
	call three;
}


procedure one{
	x = a;
	x = 3;
}

procedure two{
	x= b;
	call three;
}

procedure three{
	x=c;
	print c;
	read x;
}